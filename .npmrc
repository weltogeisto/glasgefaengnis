# react-three-fiber 9 / drei 10 geben ihre React-19-Bereiche noch nicht sauber
# an. Ohne das hier scheitert ein blankes `npm install` — auf einem fremden
# Rechner ebenso wie im Standard-Build von Vercel.
legacy-peer-deps=true
