# Overrides to the original Cycle I animation brief

`docs/MORIONDO_CYCLE_01_ANIMATION_BRIEF.md` is retained as the original production brief. It contains two instructions that have since been changed by the owner.

The current authority is `docs/STORY_CONTRACT.md`. These overrides are explicit:

## 1. Abduction flashback

The current restrained injury treatment in `mounts.abduction_flashback` is owner-approved. The mounts remain alive and recoverable. Do not regenerate this approved asset merely because the original brief requested a completely unmarked capture.

## 2. Forge structure

The existing forge casting and choreography are owner-approved:

- Ruben, Max and Hendrik participate in `phial.three_smiths_strike`;
- Max delivers the depicted principal strike;
- Hendrik appears in the approved white Kriegsfürsten armour;
- Ruben performs the fine assembly in `phial.ruben_assemble`;
- the Blaue-Glut anvil rune remains part of the sequence.

Do not restore the original single-title-holder interpretation. Missing forge assets must extend these two approved scenes inside the two-day app progression.

## 3. Precedence

For all future work, use this precedence:

1. `docs/STORY_CONTRACT.md`
2. `config/presence-approvals.json`
3. this override file
4. `docs/JAN_ARC_EXTENSION.md`
5. the original animation brief
6. historical handovers and generation logs
